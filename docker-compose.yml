version: '3.8'

services:
  caddy:
    image: caddy:2-alpine
    container_name: orris_caddy
    restart: unless-stopped
    ports:
      - "80:80"
      - "443:443"
    volumes:
      - ./Caddyfile:/etc/caddy/Caddyfile:ro
      - caddy_data:/data
      - caddy_config:/config
    depends_on:
      - frontend
      - orris

  frontend:
    image: ghcr.io/orris-inc/orris-frontend:latest
    container_name: orris_frontend
    restart: unless-stopped
    expose:
      - "80"
    depends_on:
      - orris

  orris:
    image: ghcr.io/orris-inc/orris:latest
    container_name: orris_app
    restart: unless-stopped
    expose:
      - "8080"
    volumes:
      - ./configs:/app/configs:ro
    env_file:
      - .env
    depends_on:
      mysql:
        condition: service_healthy
      redis:
        condition: service_healthy

  mysql:
    image: mysql:8.0
    container_name: orris_mysql
    restart: unless-stopped
    env_file:
      - .env
    environment:
      MYSQL_ROOT_PASSWORD: ${ORRIS_DATABASE_PASSWORD:-password}
      MYSQL_DATABASE: ${ORRIS_DATABASE_DATABASE:-orris}
      MYSQL_USER: ${ORRIS_DATABASE_USERNAME:-orris}
      MYSQL_PASSWORD: ${ORRIS_DATABASE_PASSWORD:-password}
    # Bind to localhost only: the app reaches MySQL over the compose network, so
    # there is no need to expose the database on the host's public interface.
    ports:
      - "127.0.0.1:${ORRIS_DATABASE_PORT:-3306}:3306"
    volumes:
      - ./data/mysql:/var/lib/mysql
    healthcheck:
      test: ["CMD", "mysqladmin", "ping", "-h", "localhost"]
      timeout: 20s
      retries: 10

  redis:
    image: redis:7-alpine
    container_name: orris_redis
    restart: unless-stopped
    # Bind to localhost only: Redis has no password by default, so it must not be
    # reachable from the host's public interface. The app reaches it over the
    # compose network.
    ports:
      - "127.0.0.1:${ORRIS_REDIS_PORT:-6379}:6379"
    volumes:
      - ./data/redis:/data
    healthcheck:
      test: ["CMD", "redis-cli", "ping"]
      interval: 10s
      timeout: 5s
      retries: 5
    command: redis-server --appendonly yes

volumes:
  caddy_data:
  caddy_config:
